<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :rating %>
  <%= f.number_field :rating, in: 1..5 %>

  <%= f.label :text %>
  <%= f.text_area :text %>

  <%= f.submit "Submit Review" %>
<% end %>
